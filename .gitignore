node_modules
.env
.env.production
*.log
.DS_Store

# Keep fleet photos in git so Hostinger deploys include them
# (runtime downloads are wiped on every new hbuilds version)
uploads/*
!uploads/.gitkeep
!uploads/fleet/
!uploads/fleet/**
